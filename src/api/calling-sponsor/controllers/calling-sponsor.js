'use strict';

/**
 *  calling-sponsor controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::calling-sponsor.calling-sponsor');
